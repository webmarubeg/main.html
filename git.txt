GithubとCSCodeの連携を試してみます。
現状の状態は
→ローカルリポジトリの作成＝git.init(ialize) : .gitディレクトリが作成される

次の操作は

1.ステージングエリアへの追加＝git.add[ファイル名]
VSCodeでの操作があやふやなのである（どの部分を押したらできてるのかがわかっていない

2.コミット。（リポジトリへ記録）
同上

3.同期する。
同じく

ここからは、同期した後の文章になります。
この表示をgithubで確認するためには、上記の１から３の操作を繰り返す必要があります。

２.次の段階
フォーク（コピー）して公開する流れ
